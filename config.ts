const config = {
  auth: {
    enabled: false,
  },
  authNextAuth: {
    enabled: true,
  },
  authCustom: {
    enabled: false,
  },
  payments: {
    enabled: true,
  },
};

export default config;
